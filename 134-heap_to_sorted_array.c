#include "binary_trees.h"

/**
* heap_to_sorted_array - pending.
* @heap: pending.
* @size: pending.
* Return: pending.
*/
int *heap_to_sorted_array(heap_t *heap, size_t *size)
{
	if (!heap)
		return (NULL);
	return (heap);
}

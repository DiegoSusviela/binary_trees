#include "binary_trees.h"

/**
* heap_extract - pending.
* @root: pending.
* Return: pending.
*/
heap_t *heap_insert(heap_t **root, int value)
{
	if (!root || !*root)
		return (NULL);
	return (*root);
}

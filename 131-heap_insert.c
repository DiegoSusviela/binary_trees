#include "binary_trees.h"

/**
* heap_insert - pending.
* @root: pending.
* @value: pending.
* Return: pending.
*/
heap_t *heap_insert(heap_t **root, int value)
{
	(void) value;
	if (!root || !*root)
		return (NULL);
	return (*root);
}

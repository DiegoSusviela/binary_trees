#include "binary_trees.h"

/**
* avl_remove - pending.
* @root: pending.
* @value: pending.
* Return: pending.
*/
avl_t *avl_remove(avl_t *root, int value)
{
	if (!root)
		return (NULL);
	return (root);
}
